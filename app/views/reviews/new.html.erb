<h1>New comment for <%= @restaurant.name %></h1>

<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: @collection %>
  <%= f.submit "Submit review", class: "btn btn-primary btn-lg" %>
<% end %>
