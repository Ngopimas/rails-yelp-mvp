<h1>Add a new restaurant</h1>

<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: @collection %>
  <%= f.submit "Submit restaurant", class: "btn btn-primary btn-lg" %>
<% end %>
